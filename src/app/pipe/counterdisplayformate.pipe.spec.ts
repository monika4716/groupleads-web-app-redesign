import { CounterdisplayformatePipe } from './counterdisplayformate.pipe';

describe('CounterdisplayformatePipe', () => {
  it('create an instance', () => {
    const pipe = new CounterdisplayformatePipe();
    expect(pipe).toBeTruthy();
  });
});
